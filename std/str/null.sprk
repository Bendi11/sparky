ns std::str::null {

//Return the length in bytes of a NULL-terminated string buffer
fun strlen(u8 ptr str) u32 {
    let u32 len = 0u32;
    while (*str) != 0u8 {
        str = {u8 ptr}(({u64}str) + 1u64);
        len = len + 1u32;
    };
    ret len;
}

}