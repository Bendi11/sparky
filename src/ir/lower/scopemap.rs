//! [ScopeMap] for representing a program's nested scopes and all symbols accessible to them

/// Structure representing a program's nested scopes with all defined symbols accessible to them
pub struct ScopeMap {
    
}
