
fun test() -> u8 {
    return 0xf3
}

type int = i32

fun ext __spark_syscall(u64,u64,u64,u64) -> u64
fun ext __spark_puts(*u8) -> u64
type structure = {
    u8 c,
    [5]u32 a,
    *u8 b
}


fun puts(*u8 str, u64 len) -> u64 {
    return __spark_puts(str)
    //return __spark_syscall(1u64, $u64 str, len, 0u64)
}

