type test_ty = {
    i32 a,
    { i64 b, (f32 | bool) c } b,
}

type testing_type = fun(i32) -> f32

fun main test_ty argc, testing_type a -> i32 {
    let a = "test"
    let start = 51.31
    let sum = start * 31.1
    return $i32 sum
}
