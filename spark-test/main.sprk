type test_ty = {
    i32 a,
    { i64 b, (f32 | bool) c } b,
}

type testing_type = fun(i32) -> f32
type test_enum = i32 | bool

fun main(test_ty argc, testing_type a) -> i32 {
    let testing_block = {
        let a = 3
        phi 5
    }
    let a = "test"
    let start = 51.31
    let sum = start * 31.1
    return $i32 sum
}

fun otherfun([5]u8 c) {
    let a = 3.3
    let b = "test"
    let c = &a
    mut ptr = *&c

}
