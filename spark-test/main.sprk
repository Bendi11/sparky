imp mod:puts
type str = *u8
type strbuf = *str
type structure = {
    i32 a,
    *u8 b
}

type str_or_num = i32 | str

fun ext printf(str fmt, *u8 a)

fun ext test()

fun ext main(i32 argc, **u8 a) -> i32 {
    puts("test")
    return 0
}
