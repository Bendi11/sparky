imp mod:puts
type str = *u8
type strbuf = *str
type structure = {
    i32 a,
    *u8 b
}

fun ext printf(str fmt, *u8 a)

fun ext test()

fun ext main(i32 argc, **u8 a) -> i32 {
    let struct = $structure#{
        a = 100,
        b = "testing",
    }let struct.b = 3 test()printf($str "testing", "1")printf($str ((["test", "1"])[0]), "1")mod:puts("testing")return (argc + 1 / 2)
}
