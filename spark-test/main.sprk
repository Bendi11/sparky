
type test_ty = {
    i32 a,
    { i64 b, (f32 | bool) c } b,
}

type testing_type = fun(i32) -> f32
type test_enum = i32 | bool

fun test([2]i32 t) {
    return ()
}

fun ext main(i32 argc, **u8 a) -> i32 {
    mut([2]i32) a = [5, 3]
    let b = (5, [51])
    test:([5, 2])
    return 101
}

fun ext __chkstk() {
    return ()
}
