imp mod:puts
imp mod:structure
type str = *u8
type strbuf = *str


type str_or_num = i32 | str

fun ext printf(str fmt, i32 b)

fun ext test()

fun ext main(i32 argc, **u8 argv) -> i32 {
    let cond = if true {
        phi 101
    } else {
        phi 0
    }
    return cond
}
