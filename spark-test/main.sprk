type test_ty = {
    i32 a,
    { i64 b, (f32 | bool) c } b,
}

type testing_type = fun(i32) -> f32
type test_enum = i32 | bool

fun main(i32 argc, **u8 a) -> i32 {
    {
        mut a = 0
        if a > 3 {
            break
        } else {
            continue
        }
    }
    return test:() << 2
}

fun test() -> i32 {
    return 0b00000010
}
