type test_ty = {
    i32 a,
    { i64 b, (f32 | bool) c } b,
}

type testing_type = fun(i32) -> f32
type test_enum = i32 | bool

fun main(i32 argc, **u8 a) -> i32 {
    let a = "test"
    let start = 51.31
    let sum = start * 31.1
    puts:("test")
    return $i32 101
}

fun puts(*u8 s) -> ()
