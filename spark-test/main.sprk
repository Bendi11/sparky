imp mod:puts
imp mod:structure
type str = *u8
type strbuf = *str

type testing = { *u8 a }

type str_or_num = i32 | str

fun ext printf(str fmt, i32 b)

fun ext test()

fun ext main(i32 argc, **u8 argv) -> i32 {
    let(str_or_num) test_enum = $str_or_num 0
    let b = #mod:structure {
        c = 47u8,
        a = [0x4848484848484848u32, 72u32, 72u32, 72u32, 0u32],
        b = "testing"
    }
    let testing_str = #testing { a = "1" }
    let a = "testing1"
    mod:puts($*u8 testing_str.a)
    return 0
}
