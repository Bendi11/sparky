imp up:alloc

type bytebuf_t = {
    *u8 ptr,
    usz len,
    usz cap,
}



fun extend_fit(*bytebuf_t self, usz cap) {
    if self->cap < cap {
        let a = 3
    }
}

fun push(*bytebuf_t self, u8 elem) {
    
}
